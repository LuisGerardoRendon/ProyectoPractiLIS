/**
 * Este paquete contiene todas las clases VALUATE OBJECT(VO), así como las interfaces
 * DATA ACCESS OBJECT(DAO),DATA ACCESS OBJECT IMPLEMENTS(DAOImplements)  y la clase ConexionBD.
 * Todas estas estas clases estan relacionadas con los objetos utilizados en el sistema y las
 * consultas, insersiones y  actualizaciones que se hacen a la base de datos.
 */
package modelo;
